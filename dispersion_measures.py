import pandas as pd
from df_concatenator import data

# def mean_column(df, column_group, column_mean):
#     df = df.groupby(column_group)[column_mean].mean().reset_index()
#     return df


# print(mean_column(data, "UF", "UTI_ADULTO_EXIST"))
